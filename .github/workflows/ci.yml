name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  static-tests:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - name: macOS static tests
        run: NODE="$(command -v node)" SKIP_LIVE_DOCTOR=true ./macos/tests/run-tests.sh
      - name: Windows cross-platform static tests
        run: ./windows/tests/run-tests.sh
